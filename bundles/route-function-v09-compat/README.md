# MMALS v0.9 compatibility bundle

This manifest wraps the existing public CSV files in `data/demo/` without changing their contents. It demonstrates how Replay Bundle v1 can identify and bind a legacy multi-file evidence export.

The manifest does **not** claim that missing metadata was present in the original notebook export. Values such as the source commit remain explicit compatibility metadata rather than reconstructed evidence.
